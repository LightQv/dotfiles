---
description: Compress a markdown memory file into caveman prose
---

Use the `caveman-compress` skill to compress this file: $ARGUMENTS

Use the active OpenCode model for compression and the skill's local scripts for preflight, validation, backup, and atomic installation. Preserve protected Markdown exactly.
